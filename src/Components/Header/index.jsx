export default function Header() {
  return (
    <header>
      <h1 style={{
        color: "#fff",
        textAlign: "center",
        fontSize: "3rem",
        fontWeight: "bold",
      }} >Expense <span style={{
          color: "#29bbf0"
      }}>Tracker</span></h1>
    </header>
  )
}